name: Log Analyzer Test

on:
  workflow_dispatch:

jobs:
  intentional-failure:
    runs-on: ubuntu-latest

    steps:
      - name: Generate TypeScript failure
        run: |
          echo "error TS2322: Type 'string' is not assignable to type 'number'."
          echo "npm ERR! code ELIFECYCLE"
          exit 1